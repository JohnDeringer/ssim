package org.nterlearning.usermgmt.webapp.enumeration;

/**
 * This enumeration is designed to represent the various states a process can return.
 */
public enum ProcessState {
    PROCESS_FAILED,
    PROCESS_SUCCEEDED,
    PROCESS_NOT_NEEDED;
}
